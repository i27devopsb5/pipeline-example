pipeline {
    agent {
        label 'java-slave'
    }
    stages {
        stage ('Build') {
            steps {
                echo "Welcome to Jenkins Pipeline"
                error "I will print error message"
            }
        }
    }
}
